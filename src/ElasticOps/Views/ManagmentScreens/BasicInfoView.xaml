<UserControl x:Class="ElasticOps.Views.ManagmentScreens.BasicInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1024">
    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
        
        <Grid>
            <DockPanel VerticalAlignment="Top">
            <controls:MetroProgressBar VerticalAlignment="Top"
                                       Height="15"
                                       IsIndeterminate="True"
                                       Margin="5,0,5,0"
                                       Visibility="{Binding IsRefreshing, Converter={StaticResource BooleanToVisibilityConverter}}"
                ></controls:MetroProgressBar>
            </DockPanel>
            <ItemsControl Name="ClusterHealthProperties" Margin="0,20,0,0"/>

        </Grid>
    </ScrollViewer>

</UserControl>
